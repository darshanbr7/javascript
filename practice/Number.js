let a=10.002;
console.log(Math.abs(a)) // it return the absulute value
console.log(Math.ceil(a)) // it roundup the value to next integer
console.log(Math.floor(a)) // it roundup the value to the previous value
console.log(Math.exp(10))  // it return in the form of logarithem
console.log(Math.pow(a,5)) // it rturn the power  of the value.
console.log(Math.max(1,2,3,4,5)) // it returns the maximum valuse 
console.log(Math.min(1,2,3,0,4,5,6)) // it returns the minimum value
console.log(Math.random())  // it returns the random value
console.log(Math.sqrt(19))  // it returns the Sqare root of the  value
console.log(typeof(a.toString())) // it converts to string 

